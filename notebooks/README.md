# Notebooks

Exploratory Jupyter notebooks that reproduce ad-hoc analyses and figure
prototypes. Notebooks are numbered by chapter for navigation but are not
part of the numbered end-to-end pipeline (that lives in `scripts/`).
